"""
Predictive coding subpackage — Pillar 1 (Hierarchical Predictive Coding).

LevelPredictor / ErrorUnit ABCs live in `base.py`; the concrete stack and
the edge-map level live in `hierarchical_stack.py`; non-learnable
feature-target extractors (edge / orientation / shape) live in
`feature_targets.py`.
"""
